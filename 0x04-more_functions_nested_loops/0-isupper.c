#include "main.h"
/**
 * _isupper - print  uppercase character
 * @c: check upper case character
 */
int _isupper(int c)

{
	if (c > A) || (c < Z)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
