#include "main.h"
/**
* print_numbers - iis it digit
* Return: Always 0 (Success)
*/
void print_numbers(void)

{
	int c;

	if (c >= 48 && c <= 57)
	{
		_putchar(c);
	}
	else
	{
		_putchar(c);
	}
	_putchar('\n');
}
